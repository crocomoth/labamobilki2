<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:labamobilki2"
             x:Class="labamobilki2.MainPage"
             Title="hearts of iron 4">

    <ScrollView IsEnabled="True">
        <StackLayout>
            <ListView x:Name="ItemListView" 
					  HasUnevenRows="True"
					  ItemTapped="ItemListView_OnItemTapped"
                      ItemsSource="{Binding MainList.Elements}"
					  SeparatorVisibility="Default">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:ItemViewCell>
                            <Image Source="{Binding Image}"></Image>
                            <Label Text="{Binding Name}"></Label>
                            <Label Text="{Binding Shortdescr}"></Label>
                            <Label Text="{Binding LongDescr}" ></Label>      
                        </local:ItemViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ScrollView>

</ContentPage>
